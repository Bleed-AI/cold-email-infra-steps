import type { SetupVariantProps } from "./types";

/** Sample client so each variant renders personalized without the Entry form. */
export const SAMPLE: SetupVariantProps = {
  businessName: "Acme",
  slug: "acme",
  mainDomain: "acme.com",
};
